<div class="messages">
  {{#each this.messages as |message|}}
    <Message
      @username={{message.username}}
      @userIsActive={{message.active}}
      @userLocalTime={{message.lacalTime}}
    >
      {{{message.content}}}
    </Message>
  {{/each}}

  <NewMessageInput @onCreate={{this.addMessage}} />
</div>